{
  "logLevel": "info",
  "api": {
    "port": 8989
  },
  "nodemailer": {
    "host": "{{host}}",
    "secure": true,
    "port": {{port}},
    "auth": {
      "user": "{{user}}",
      "pass": "{{password}}"
    }
  },
  "fromEmail": "{{from_email}}",
  "toEmail": "{{to_email}}"
}
